# SiteController handles the frontend for the website
class SiteController < ApplicationController
  def index
    # show the front page
  end

  def redirect
    # track the click and redirect to vendor's cart / product page
  end
end
